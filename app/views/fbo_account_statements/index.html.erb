<article>
  <h1>FBO Account Statements: <%= @account.name %></h1>
  <nav>
    <ul>
      <li>
        <%= link_to "New", new_fbo_account_statement_path(@account) %>
      </li>
    </ul>
  </nav>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>Filename</th>
        <th>Uploaded</th>
      </tr>
    </thead>
    <tbody>
      <% @statements.each do |statement| %>
        <td><%= statement.id %></td>
        <td><%= statement.original_filename %></td>
        <td><%= statement.created_at.to_s %></td>
      <% end %>
    </tbody>
  </table>

  <ul>

  </ul>
</article>