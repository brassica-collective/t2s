<article>
  <h1>New TE Participant</h1>
  <%= form_with model: @participant do |form| %>
    <p>
      <%= form.label :name %>
      <%= form.text_field :name %>
    </p>
    <p>
      <%= form.submit "Submit" %>
    </p>
  <% end %>
</article>